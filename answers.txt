What is the difference between a merge and a rebase?
    A merge is non-destructive and preserves the history of the repository
    while a rebase moves the feature branch onto the tip of the main branch
    making a more linear history.